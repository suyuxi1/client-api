spring:
  profiles:
    active: dev

  # jackson����
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  # ����Դ����
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://rm-m5ee476bu350735gjeo.mysql.rds.aliyuncs.com:3306/first_smart_campus?useUnide=true&characterEncoding=UTF-8&useSSL=false&autoReconnect=true
    username: root
    password: XuNiit_#
#    url: jdbc:mysql://localhost:3306/smart_campus?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&useSSL=false
#    username: root
#    password: root
    hikari:
      # ��С������
      minimum-idle: 1
      # ���������
      maximum-pool-size: 20
      # �Զ��ύ
      auto-commit: true
      # ���ӳ�ʱ�����ʱ�������룩����ʱ���ͷţ�retired����Ĭ��:10����
      idle-timeout: 10000
      # ���ӵ�����ʱ�������룩����ʱ����û��ʹ�����ͷţ�retired����Ĭ��:30���� 1800000ms
      max-lifetime: 1800000
      # �ȴ����ӳط������ӵ����ʱ�������룩
      connection-timeout: 30000
      # ���Ӳ��Բ�ѯ���
      connection-test-query: SELECT 1
  #      jpa
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  redis:
    database: 0
    password:
    port: 6379
    host: localhost
    lettuce:
      pool:
        min-idle: 5
        max-idle: 10
        max-active: 8
        max-wait: 1ms
      shutdown-timeout: 100ms